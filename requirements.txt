# TemporalEdge — Phase 6 deployment requirements
# Pinned for Streamlit Cloud (Python 3.11, linux/x86_64)
# Do NOT add fastapi/uvicorn — not needed on Streamlit Cloud

# Data
yfinance>=0.2.40
pandas>=2.0.0
numpy>=1.24.0
pandas-datareader>=0.10.0
pyarrow>=14.0.0
fastparquet>=2023.0.0

# Technical indicators
ta>=0.11.0

# ML
lightgbm>=4.0.0
scikit-learn>=1.3.0

# Experiment tracking — disabled on cloud (no local DB), kept for import compat
mlflow>=2.8.0

# LLM (Groq — free tier)
groq>=0.4.0

# Dashboard
streamlit>=1.28.0
plotly>=5.17.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0